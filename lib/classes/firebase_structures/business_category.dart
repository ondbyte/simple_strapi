/* class BusinessCategory {
  final String name;
  final String image;
  final bool enabled;

  BusinessCategory({this.name, this.image, this.enabled});

  static BusinessCategory fromJson(Map<String, dynamic> j) {
    return BusinessCategory(name: j["name"], image: j["image"], enabled: j["enabled"]);
  }

  Map<String, dynamic> toMap() {
    return {
      "name": name,
      "image": image,
      "enabled": enabled,
    };
  }
}
 */